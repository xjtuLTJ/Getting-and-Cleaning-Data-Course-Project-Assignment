###  Data in code:

```R
train: Training set.
test: Test set.
subject_train: Each row identifies the subject who performed the activity for each window sample. Its range is from 1 to 30. 
subject_test: Each row identifies the subject who performed the activity for each window sample. Its range is from 1 to 30. 
label_train: Training labels.
label_test: Test labels.
trainData: Including the training set, mean of each row, standard deviation of each row, data type , subject of training set, and labels of training set.
```



### Variables in allData:

```
mean: the mean of each row in data set.
std: the standard deviation of each row in data set.
dataType: The type（train or test) of each row in the original data set.
```

